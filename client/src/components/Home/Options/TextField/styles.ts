import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  paper: {
    display: "flex",
    alignItems: "center",
    backgroundColor: "lightgrey",
    color: "grey",
    padding: "5px",
  },
}));
